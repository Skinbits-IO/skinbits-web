export type User = {
  name: string;
  surname: string;
  rank: string;
  tapLevel: number;
  fuelLevel: number;
  farmLevel: number;
  balance: number;
  totalEarned: number;
  photoUrl: string | null;
};
